{
  "name": "@glade/glfw",
  "version": "0.0.1",
  "type": "module",
  "main": "glfw.ts",
  "types": "glfw.ts",
  "scripts": {
    "lint": "eslint . --fix",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "nuke": "rm -rf node_modules"
  },
  "dependencies": {
    "@glade/logging": "workspace:*"
  }
}
