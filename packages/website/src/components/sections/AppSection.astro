---
import SectionTag from "../SectionTag.astro";
import ImagePlaceholder from "../ImagePlaceholder.astro";
---

<section id="app" class="scroll-mt-24 flex flex-col gap-6">
  <SectionTag>Root</SectionTag>
  <h2 class="-mt-4 mb-4">App</h2>
  <h3>Application lifecycle and configuration</h3>
  <p class="text-gray-600">
    The App class is the entry point for every Glade application. It manages the application lifecycle, handles window creation, and orchestrates the rendering pipeline. Configure your app with themes, plugins, and custom settings.
  </p>
  <ImagePlaceholder />
  <hr class="mt-10 bg-gray-300" />
</section>
