---
import FeatureRow from "../FeatureRow.astro";
---

<section id="features" class="py-20 border-b border-gray-100 bg-white scroll-mt-8">
  <div class="mx-auto max-w-3xl px-3">
    <h2 class="font-mono text-sm uppercase tracking-wider text-gray-400 mb-8">03 — Features</h2>
    <p class="text-gray-700 leading-relaxed mb-6">
      A quick overview of what Glade can, can't, doesn't, won't, or could do, and why. For more
      information on the "why" or "how" please read the README.md section below this one
    </p>
    <div class="border-2 border-gray-500 rounded-md overflow-hidden bg-white">
      <!-- Header Row -->
      <div class="grid grid-cols-10 border-b-2 border-gray-500">
        <div
          class="col-span-2 px-2 py-1 text-[13px] font-mono uppercase tracking-wider text-gray-900 font-bold"
        >
          Name
        </div>
        <div
          class="col-span-1 px-2 py-1 text-[13px] font-mono uppercase tracking-wider text-gray-900 border-l-2 border-gray-500 font-bold"
        >
          Status
        </div>
        <div
          class="col-span-7 px-2 py-1 text-[13px] font-mono uppercase tracking-wider text-gray-900 border-l-2 border-gray-500 font-bold"
        >
          Details
        </div>
      </div>

      <!-- Data Rows -->
      <FeatureRow
        name="WebGPU renderer"
        info="done"
        details="WebGPU in the browser and Dawn on native; scene-based render loop with ordered layers."
      />
      <FeatureRow
        name="Cross-platform"
        info="done"
        details="macOS + web targets from the same TypeScript codebase."
      />
      <FeatureRow
        name="Taffy layout"
        info="partial"
        details="Flexbox + grid via WASM. Note: layout is serialized every frame; todo: cache unchanged constraints to avoid reflow."
      />
      <FeatureRow
        name="Font rendering"
        info="done"
        details="rustybuzz shaping, glyph caching, and font fallback; Noto Color Emoji default."
      />
      <FeatureRow
        name="SVG rendering"
        info="partial"
        details="Lyon-based path parsing; renders SVGs fairly well. Known gaps around full spec edge cases."
      />
      <FeatureRow
        name="Elements + components"
        info="partial"
        details="Core elements plus buttons, tooltips, tabs, scrollbars, dialogs, inputs; more in progress."
      />
      <FeatureRow
        name="Utilities"
        info="done"
        details="Tailwind-style utility props on elements and components."
      />
      <FeatureRow
        name="Text editing"
        info="done"
        details="Selection, range editing, select-all, and clipboard copy/paste supported."
      />
      <FeatureRow
        name="Static binaries"
        info="done"
        details="Bun FFI + file embedding for single, self-contained executables."
      />
      <FeatureRow
        name="Custom cursors"
        info="done"
        details="Tailwind-like cursor set: pointer, text, default, and more."
      />
      <FeatureRow
        name="Typography components"
        info="todo"
        details="Common primitives like p(), em(), strong(), span(), and kbd()."
      />
      <FeatureRow
        name="Directional borders"
        info="todo"
        details="Per-side borders on divs and other components."
      />
      <FeatureRow
        name="Drag and drop"
        info="todo"
        details="Pointer-driven drag, drop targets, and transfer state."
      />
      <FeatureRow
        name="Text ellipsis"
        info="todo"
        details="Truncation and ellipses for overflowing text."
      />
      <FeatureRow
        name="Rich text composition"
        info="todo"
        details="Composing text in one line like you would with <p>, <span>, <strong> and so on."
      />
      <FeatureRow
        name="120fps target"
        info="partial"
        details="Depends on components on the page. Demos run between 60 and 120."
      />
      <FeatureRow
        name="Glyph serde"
        info="improvement"
        details="Using font shaping in WASM adds overhead per frame"
      />
      <FeatureRow
        name="Layout caching"
        info="improvement"
        details="Could improve rendering speed by around 70%"
        isLast={true}
      />
    </div>
  </div>
</section>
