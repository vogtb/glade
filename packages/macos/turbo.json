{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build.debug": {
      "outputs": ["../../libs/libgladehelpers.dylib"]
    },
    "build.release": {
      "outputs": ["../../libs/libgladehelpers.dylib"]
    }
  }
}
