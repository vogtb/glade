{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build.debug": {
      "outputs": ["pkg/**"]
    },
    "build.release": {
      "outputs": ["pkg/**"]
    }
  }
}
