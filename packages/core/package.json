{
  "name": "@glade/core",
  "version": "0.0.1",
  "type": "module",
  "main": "core.ts",
  "types": "core.ts",
  "scripts": {
    "lint": "eslint . --fix",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "nuke": "rm -rf node_modules"
  }
}
